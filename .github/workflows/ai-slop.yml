name: AI Slop Detect
on:
  pull_request:
    types: [opened]
  issues:
    types: [opened]
permissions:
  issues: write
  pull-requests: write
jobs:
  detect:
    runs-on: ubuntu-latest
    steps:
      - name: AI Slop Detector
        uses: ./
